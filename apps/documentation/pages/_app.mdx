import './globals.css';
import { AppProps } from 'next/app';
import { Inter, Fira_Code, Vollkorn } from 'next/font/google';
import { Analytics } from '@vercel/analytics/react';
import { SpeedInsights } from "@vercel/speed-insights/next"
import { Toaster } from '@commonalityco/ui-design-system/toaster';

export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
});

export const firaCode = Fira_Code({
  subsets: ['latin'],
  variable: '--font-fira-code',
});

export const vollkorn = Vollkorn({
  subsets: ['latin'],
  variable: '--font-vollkorn',
  style: ['normal', 'italic'],
});

export default function MyApp({ Component, pageProps }) {
  return (
    <div
      className={`${inter.className} ${firaCode.variable} ${inter.variable} ${vollkorn.variable}`}
    >
      <Component {...pageProps} />
      <Analytics />
      <SpeedInsights />
      <Toaster />
    </div>
  );
}
