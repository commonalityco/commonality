import { Onest, Fira_Code, Inter } from 'next/font/google';

export const onest = Onest({
  subsets: ['latin'],
  variable: '--font-inter',
});

export const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
});

export const firaCode = Fira_Code({
  subsets: ['latin'],
  variable: '--font-fira-code',
});
